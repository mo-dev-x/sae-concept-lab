__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/
.venv/
venv/
node_modules/

# Everything "removed" from the public build lands here instead of being
# deleted -- see the repository's own publication notes. Never a source
# of truth, never re-added to a tracked path without review.
_attic/

# Local secrets -- never committed, regardless of which tool wrote them.
.env
.env.*
.hf_token
hf_token.txt

# Generated by sae_concept_lab.smoke.pi_demo_preflight/tamia_smoke; each
# carries an absolute repo_root or a local weights path, so these are
# local run artifacts and never committed ones.
pi_demo_preflight_result.json
pi_demo_preflight_mode_a.json
tamia_smoke_packet.json

# Model/SAE weights and other large run outputs are never staged here.
bundles/output/
*.safetensors
*.sae.pt
